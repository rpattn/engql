server:
  port: 8080
  host: "localhost"
  internal_secret: "change-me-in-prod"

database:
  user: "postgres"
  password: "admin"
  name: "engineering_api"
  host: "localhost"
  port: 5432

options:
  enable_playground: true